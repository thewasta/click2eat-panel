version: '3.4'
services:
  app:
    build:
      dockerfile: docker/app/app.dockerfile
      context: .
    volumes:
      - /app
    ports:
      - '3000:3000'
    environment:
      - NODE_ENV=production
      - DOTENV_KEY=${DOTENV_KEY}